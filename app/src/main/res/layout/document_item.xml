<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_marginTop="@dimen/activity_margin_half"
    android:layout_height="wrap_content">

    <com.tanmay.androidsupport.utils.SquareImageView
        android:id="@+id/di_doc"
        android:layout_width="@dimen/doc_image"
        android:layout_height="@dimen/doc_image"
        android:layout_centerHorizontal="true"
        android:fitsSystemWindows="true"
        android:scaleType="centerCrop"
        app:layout_collapseMode="parallax" />

    <TextView
        android:id="@+id/di_doc_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/di_doc"
        android:layout_centerHorizontal="true"
        android:text="adssdfgassgkagdhs hsadfhka"
        android:textColor="@android:color/black" />

</RelativeLayout>